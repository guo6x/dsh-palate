{
  "name": "dsh-palate",
  "version": "0.3.3",
  "description": "An eye that grows: accumulated design taste for DSH agents. A taste corpus + codified principles that sharpen with every example you feed — review designs against learned judgment, not a fixed ruler.",
  "type": "module",
  "main": "lib/index.js",
  "exports": {
    ".": { "default": "./lib/index.js" },
    "./client": { "default": "./lib/client.js" },
    "./package.json": "./package.json"
  },
  "files": [
    "lib",
    "src",
    "cordis.patch.yml",
    "dsh.plugin.json"
  ],
  "dsh": {
    "bundle": { "patch": "./cordis.patch.yml" },
    "client": {
      "inject": ["slots", "locale", "layout", "connection"],
      "platform": "web"
    }
  },
  "scripts": {
    "build": "node build.mjs",
    "test": "node build.mjs && node tests/smoke.mjs && node tests/plugin-contract.mjs && node tests/client-contract.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "keywords": [
    "dsh",
    "dsh-plugin",
    "deepseek-harness",
    "design",
    "taste",
    "aesthetic",
    "review",
    "critique"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/guo6x/dsh-palate.git"
  }
}
